import { defineConfig } from "@playwright/test";

export default defineConfig({
  // Add custom Playwright configuration overrides here
  // Example:
  // timeout: 60000,
  // use: {
  //   baseURL: "http://localhost:3000",
  // },
});
